# Copy to .env at the repo root and fill in. .env is git-ignored.
# The docker-compose backend service reads this file (env_file: .env).

# Required: Claude API key used for rubric evaluation.
ANTHROPIC_API_KEY=

# Optional: whisper model size (tiny | base | small | medium | large-v3). Default: base
WHISPER_MODEL=base
